namespace CrynticCompiler;

public enum CompilationMode
{
    /// <summary>
    /// used for debugging the compiler
    /// </summary>
    Debug,
    /// <summary>
    /// used when the compiler is released
    /// </summary>
    Release
}